---
- name: Install htop locally
  hosts: localhost
  become: yes
  tasks:
    - name: Install htop
      apt:
        name: htop
        state: present
        update_cache: yes
        cache_valid_time: 3600
